<li class="list-group-item">
	{{burger_name}}

	<button class="change-devoured btn btn-warning" data-id="{{id}}" data-newdevoured="{{devoured}}" type="button">
		{{#if devoured}}Devoured!{{else}}NOT Devoured!{{/if}}
	</button>

	<button class="delete-burger btn btn-success" data-id="{{this.id}}" type="button">DELETE!</button>
</li>
